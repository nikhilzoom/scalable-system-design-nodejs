apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ip-api-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ip-api
                port:
                  number: 3000
